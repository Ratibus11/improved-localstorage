// MODULES
// Assertion
import { expect } from "chai";

// Used to test if the parameter is strictly `true`
const shouldBeTrue = (functionToTest: (...args: any) => any) => {
    describe("key - Entry to remove's key.", () => {});
};

const shouldBeFalse = (functionToTest: (...args: any) => any) => {
    describe("key - Entry to remove's key.", () => {});
};

export { shouldBeFalse, shouldBeTrue };
